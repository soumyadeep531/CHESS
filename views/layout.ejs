<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chess Game</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body class="bg-zinc-900 text-white min-h-screen flex flex-col">
    <nav class="bg-zinc-800 p-4 shadow-md">
        <div class="container mx-auto flex justify-between items-center">
            <a href="/" class="text-2xl font-bold text-orange-500">ChessMaster</a>
            <div class="space-x-4 flex items-center">
                <% if (user) { %>
                    <span class="text-zinc-400 mr-2">Welcome, <%= user.username %></span>
                    <a href="/dashboard" class="hover:text-orange-400">Dashboard</a>
                    <a href="/logout"
                        class="bg-red-600 hover:bg-red-700 px-3 py-1 rounded text-sm font-bold transition">Logout</a>
                    <% } else { %>
                        <a href="/" class="hover:text-orange-400">Home</a>
                        <a href="/login" class="hover:text-orange-400">Login</a>
                        <a href="/signup" class="hover:text-orange-400">Signup</a>
                        <% } %>
            </div>

        </div>
    </nav>

    <main class="container mx-auto flex-grow p-6">
        <%- body %>
    </main>

    <footer class="bg-zinc-800 text-center p-4 mt-auto">
        <p class="text-zinc-500">&copy; 2024 ChessMaster. All rights reserved.</p>
    </footer>
</body>

</html>